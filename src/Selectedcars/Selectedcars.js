import React from 'react';
import './Selectedcars.css'

const Selectedcars = (props) => {
    const {selectedcars} = props;
    return (
        <div>
            <h1>Selected Cars</h1>
        </div>
    );
};

export default Selectedcars;